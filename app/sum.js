// You should create an array of numbers
// in a Javascript file, if there isn't one.
//  Write a function that generates a sum.

var ary = [1,2];

function sumDis(arr) {
  let sum = 0;
  sum = arr[0] + arr[1];
  return sum;
}
